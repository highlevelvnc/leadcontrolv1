# ════════════════════════════════════════════════
# LeadControl SaaS — Variáveis de Ambiente
# Copie para .env e preencha os valores reais
# ════════════════════════════════════════════════

# ── Banco de Dados ──────────────────────────────
DATABASE_URL="postgresql://leadcontrol:leadcontrol_secret@localhost:5432/leadcontrol?schema=public"

# Credenciais Postgres (usadas pelo docker-compose)
POSTGRES_USER=leadcontrol
POSTGRES_PASSWORD=leadcontrol_secret
POSTGRES_DB=leadcontrol

# ── JWT ─────────────────────────────────────────
JWT_SECRET=troque_por_uma_chave_longa_e_aleatoria_minimo_64_chars
JWT_EXPIRES_IN=7d

# ── Servidor ────────────────────────────────────
PORT=3000
NODE_ENV=development

# ── CORS ────────────────────────────────────────
# Em produção: https://seudomain.com
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173

# ── Tenant padrão (seed) ─────────────────────────
SEED_TENANT_NAME="LeadControl Demo"
SEED_TENANT_SLUG="leadcontrol-demo"
SEED_ADMIN_EMAIL=admin@leadcontrol.pt
SEED_ADMIN_PASSWORD=admin123

# ── Futuro: e-mail (Resend / SendGrid) ──────────
# RESEND_API_KEY=re_...
# EMAIL_FROM=noreply@leadcontrol.pt

# ── Futuro: WhatsApp Business API ───────────────
# WHATSAPP_TOKEN=...
# WHATSAPP_PHONE_ID=...

# ── Futuro: Stripe (SaaS billing) ───────────────
# STRIPE_SECRET_KEY=sk_live_...
# STRIPE_WEBHOOK_SECRET=whsec_...
